import products from "../../productsStore.js";

export default async function filterByName(productList){

    // let countValue;
    // products.subscribe((data) => {
    //     countValue = data;
    // });

    // console.log("Hello: ", countValue)
}

